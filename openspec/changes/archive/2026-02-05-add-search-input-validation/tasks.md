## 1. Add date filter validation

- [x] 1.1 Add validation to reject `date` used with `date_start` or `date_end`
- [x] 1.2 Add validation to reject `date_start` > `date_end`

## 2. Add amount filter validation

- [x] 2.1 Add validation to reject `amount` used with `amount_min` or `amount_max`
- [x] 2.2 Add validation to reject `amount_min` > `amount_max`

## 3. Verify

- [x] 3.1 Run existing tests to ensure no regressions
- [x] 3.2 Verify error messages are clear and helpful
